----Swift.(file).Array
toString() {
    return '[' + this.join(', ') + ']'
}
subscript$set($info, el, index) {
    this[index] = _cloneStruct(el)
}
----closing dashes needed for the last definition to be processed